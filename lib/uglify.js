'use strict';
const fs = require('fs');
const Path = require('path');
const mkdir = require('mkdirp');
const PATH = require('./path');
const uglifyJS = require('uglify-js');

let uglify = function (config, cb) {
    
};
exports.uglify= uglify; 
